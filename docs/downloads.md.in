---
description: Download Kube Inspector — the free Kubernetes desktop client — and the kube-inspector-cli terminal UI for Windows, macOS, and Linux.
---

# Downloads

Latest installers and packages for **v__VERSION__**.

Kube Inspector ships in two flavours:

- **Desktop App** — the full graphical client.
- **Command-Line (CLI)** — `kube-inspector-cli`, a terminal UI with the same features, ideal over SSH. See the [CLI docs](cli/index.md).

!!! tip "Verify your download"
    Each file has a `.sha256` sidecar, and the whole release is covered by
    [`SHA256SUMS-__VERSION__`](/dist/SHA256SUMS-__VERSION__). The builds are
    unsigned, so this is the one integrity check available —
    [how to verify](getting-started/verifying-downloads.md).

Both are self-contained: **no runtime dependencies to install on any platform**.

---

## Desktop App

### Linux

One package per family — there is no per-distribution variant to choose.

- **Debian / Ubuntu (.deb)** — [Download](/dist/kube-inspector-__VERSION__-linux-amd64.deb)
- **RHEL / Fedora (.rpm)** — [Download](/dist/kube-inspector-__VERSION__-linux-x86_64.rpm)

### Windows

- **Windows (installer)** — [Download](/dist/kube-inspector-__VERSION__-windows-amd64.exe)

### macOS

- **macOS (Apple Silicon)** — [Download](/dist/kube-inspector-__VERSION__-macos-arm64.dmg)

---

## Command-Line (CLI)

A single self-contained binary — one Linux package fits every distro. See [CLI Installation](cli/installation.md) for setup details.

### Linux

- **Portable binary (.tar.gz)** — [Download](/dist/kube-inspector-cli-__VERSION__-linux-amd64.tar.gz)
- **Debian / Ubuntu (.deb)** — [Download](/dist/kube-inspector-cli-__VERSION__-linux-amd64.deb)
- **RHEL / Fedora (.rpm)** — [Download](/dist/kube-inspector-cli-__VERSION__-linux-x86_64.rpm)

### Windows

- **Windows** — [Download](/dist/kube-inspector-cli-__VERSION__-windows-amd64.exe)

### macOS

- **macOS (Universal)** — [Download](/dist/kube-inspector-cli-__VERSION__-macos-universal.tar.gz)
